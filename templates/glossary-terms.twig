{% block terms %}
    <ul class="terms-list">
        {% for term in terms %}
            {% block term %}
                {{ include('glossary-terms-term.twig', {
                    term: term,
                    translations: translations,
                    languages: languages
                    }, with_context = false) }}
            {% endblock %}
        {% endfor %}
    </ul>
{% endblock %}